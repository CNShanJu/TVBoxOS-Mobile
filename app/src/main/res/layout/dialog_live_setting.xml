<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bg"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_gravity="center"
    android:background="@color/bg_popup"
    android:clipToOutline="true">

    <!-- 毛玻璃模糊层: 只模糊弹层自身区域的下方内容(FrostedGlassUtil 自动铺满弹层实际大小) -->
    <eightbitlab.com.blurview.BlurView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:tag="glass_blur" />

    <!-- 半透明主题叠色: 透明度由主题文件 bg_popup_alpha 统一控制 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/bg_popup" />

    <!-- 内容容器用 match_parent 高度: 保证在 FrameLayout 下测量方式与原先一致, 内容不被压缩 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingTop="20dp"
        android:paddingBottom="10dp"
        android:gravity="center_horizontal">

        <com.owen.tvrecyclerview.widget.TvRecyclerView
            android:id="@+id/mSettingGroupView"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:padding="20dp"
            android:gravity= "center"
            app:tv_selectedItemIsCentered="true"
            app:tv_verticalSpacingWithMargins="10dp" />


        <com.owen.tvrecyclerview.widget.TvRecyclerView
            android:id="@+id/mSettingItemView"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent"
            android:padding="20dp"
            android:visibility="visible"
            android:gravity= "center"
            app:tv_selectedItemIsCentered="true"
            app:tv_verticalSpacingWithMargins="10dp" />

    </LinearLayout>

</FrameLayout>
