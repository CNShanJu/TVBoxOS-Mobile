# 下一轮任务交接（安全审计修复）

> 用途:给新对话的起点,避免重复上下文。**当前目标 = 修复 TVBoxOS-Mobile 安全审计报告全部问题**
> (第一轮 P0 + 第二轮 P0/P1/P2),代码级自查 + Gradle 编译验证,改动自洽不引入新崩溃。

## 0. 开场白(可直接贴给新会话)

```
你是 TVBoxOS-Mobile(Android TVBox 系,MBox)的开发者。当前任务是**修复安全审计报告中的全部问题**,
分两轮执行,每轮完成后代码级自查并跑 Gradle 门禁。
项目规则文件 AGENTS.md 已固化《改进.txt》约束 + 安全/工程红线,先读它再动手。
门禁命令(每批提交前必须全绿):
  .\gradlew.bat :app:assembleDebug :app:assembleRelease :app:testDebugUnitTest checkModuleDependencies
提交遵循 doc/Git提交规范.md(type[scope]: ≤50字;一次提交一件事;文件级 git add)。
注意:仓库根 改进.txt、release-notes-v3.0.1.md、app-cc.txt 永不提交;.idea/compiler.xml 不提交。
```

## 1. 待修问题清单(按轮)

**第一轮 P0**
1. 局域网服务未授权读写 / 目录穿越(RemoteServer 等回环 HTTP)
2. Zip Slip(解压归档条目路径校验;当前有 3 处 Zip/解压点需审)
3. 全局禁用 HTTPS 校验(WebView onReceivedSslError / HttpClient 信任;当前 proceed 点 1 处,SysWebClient 已在 PlayParseCoordinator,默认拒绝——复核其余 HttpClient/下载/播放器)
4. 明确崩溃点(代码级自查:可空/越界/类型转换)
5. Room 主线程查询
6. Android 暴露面(Manifest 权限/exported)
7. 超大类拆分(见 §4 行数现状)
8. 依赖升级
9. 签名配置(仓库已改 mbox 专属签名 + Secrets,核对 build.gradle/keystore 不入库)

**第二轮 P0/P1/P2**
- 启动删缓存、HLS 落盘节流、Gson 复用与索引、启动懒加载、UA 数组化
- 下载页 DiffUtil、LocalVideoAdapter O(n²)、GridFragment 视图泄漏
- 搜索线程池、网络监听统一(SystemStateMonitor)、局域网 HTTP 按需启动
- 图片库统一、冗余依赖与源码清理、Manifest 权限
- Gradle 缓存与并行、语言资源限制(resConfigs)、Room 版本对齐

## 2. 代码级自查要点(改完每批执行)

- 遍历改动文件:null 判空/集合越界/JSON 键缺失/WebView 生命周期/Activity 泄漏
- grep 复核:主线程 Room 调用、`(Activity) context` 强转、新 EventBus 事件、`newFixedThreadPool`、
  未授权文件读写;禁止再引入
- 编译:先 `:app:compileDebugJavaWithJavac` 快速试,再跑完整门禁

## 3. 规则与参考文档(已整理)

- 根目录 `AGENTS.md`:改进.txt 约束 + 安全红线(网络/TLS、路径/Zip Slip、Room 主线程、
  Manifest 暴露面、依赖对齐等)——**新代码必须遵守**
- `doc/gap-vs-improvements.md`:改进.txt 逐项审计与处理记录(§10 按轮追加,新批次在此登记)
- `doc/device-regression-checklist.md`:真机回归清单(需设备,门禁绿后过)
- `doc/Git提交规范.md`:提交格式
- `doc/spider-strong-type-design.md`:强类型 SpiderService 设计(删字符串通道时参照)
- `doc/audit-fixes-and-refactor-status.md` / `doc/phase2-core-modules-plan.md`:历史审计与模块计划
- 已删除过时文档:`doc/roadmap-improvements.md`(并入 gap)、`doc/回归测试清单.md`
  (被 device-regression-checklist 取代)、`doc/backup/`(PlayParseHelper.wip 已被
  util/player/PlayParseCoordinator 落地;若需可重建)

## 4. 改进.txt 改造进度(截至交接)

架构方向已完成的大块:core-model/core-network/core-storage/spider-api/player-api 模块齐位与门禁;
app 无 getCSP/Hawk 直读、download internal + Facade、契约 Providers(Source/Parse/Live/Loader/IJK)
注入、DataStore 化(Hawk 仅迁移通道)、EventBus 大量收口、弹窗协调器 + 共享 Panel、
PlayRequest/PlaySessionKeys/PlayedVodKey/SubtitleCoordinator/PlayHistoryRepository/PlayParseCoordinator。

大文件行数现状(拆分目标,实测于 HEAD 701bccb8/2026-09-06):PlayFragment 1010、DetailActivity 1063、
DownloadFragment 1082、SourceViewModel 949、LiveActivity 883(均需继续薄化,阶段三,需真机回归背书;
SourceViewModel 行数含 K5 typed-first 的 fetch*HttpLegacy 逐字兜底)。

已收口大块(截至 2026-09-06):hawk 退役 N/N+1(KeyValueStore 与 legacy 分支删除)、EventBus 按需注册
(仅 Detail/FastSearch/PlayService/DownloadFacade 桥)、type0/1/4 六入口 typed-first(detail/search/
quickSearch/play/category/list,HttpSourceParams+HttpUrls+AbsXmlParser,旧路径逐字兜底,评估 §J/K5;
Home sort 待设备批)、后台播放系统 MediaSession 媒体卡(ba7e685b)、K4 设备回归 G/H 组结案。

进行中/长线:播放器 PlayerApi 会话全驱动 P2-P4(实验开关,默认关)、DetailActivity/PlayFragment 大拆分、
Home sort typed-first(设备批)、feature 模块化、Media3/Hilt(五阶段)。

## 5. 工作区状态提醒(交接时)

- 最近提交链(截至 701bccb8/2026-09-06):701bccb8(docs §J/K5 登记)、724f7514(category typed-first)、
  8c7b5b9b(type4 play 拼参)、857db402(search/quickSearch typed-first)、a1798559(detail typed-first)、
  fe58a119(HttpUrls/带参 getSync)、ba7e685b(后台播放 MediaSession)、f9bd27d6(IJK FFmpegApi 恢复);
  更早:851c13eb(AGENTS 安全红线)、d2d8b707(删过时文档)、b98de4bc(PlayParseCoordinator)、
  97aadf32(线程池收口)、73060c3a(直播弹窗去强转)
- 可能有并行改动未提交(换包名 mbox/签名/CI/SelectDialog/HomeFragment 等),先 `git status` 看;
  只按自己范围 `git add`,别混入无关改动
- 构建/临时文件目录:build/ 不入库;提交信息临时文件放 build/ 即可
