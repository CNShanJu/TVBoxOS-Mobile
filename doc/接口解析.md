# 接口(订阅)解析与问题排查文档

> 目的:记录 MBox 的订阅/接口解析机制与排查经验,方便日后复查。
> 起因:2026-08-15 排查"饭太硬"源一直解析不到的问题,沉淀核心结论与排查方法。

---

## 1. 概述

MBox 的"订阅"(饭太硬、摸鱼儿、老刘备等)本质上是一个**远程 JSON 配置**,里面声明了若干内容源(site)。App 启动时拉取该配置,解析出源列表 → 每个源可提供:分类、筛选、搜索、详情、播放地址。

解析链路:

```
订阅地址(远程 JSON)
  → HttpClient 拉取原始字节
  → FindResult() 解码(去 BOM/注释、JPEG+**+base64 套路、AES 加密)
  → parseJson() 解析出 sites/parses/flags
  → 每个 site 按 type 决定内容获取方式(HTTP 接口 / Spider jar)
```

---

## 2. 订阅配置结构

顶层 JSON 三个核心数组(见 `ApiConfig.parseJson`):

```json
{
  "sites": [
    {
      "key": "fan_tai_ying",        // 源唯一标识,详情页/下载都用它
      "name": "饭太硬",             // 显示名,下载目录一级文件夹用它
      "type": 3,                    // 0=XML接口 1=JSON接口 3=Spider(jar) 4=JSON?
      "api": "http://.../api.php/provide/vod/",
      "searchable": 1, "quickSearch": 1, "filterable": 1,
      "playUrl": "", "ext": "", "jar": "http://.../spider.jar",
      "playerType": -1, "categories": []
    }
  ],
  "parses": [ { "name": "...", "url": "...", "type": 0 } ],
  "flags":   [ "解析flag列表" ]
}
```

关键字段:
- `key`:全局唯一,历史记录/收藏/下载均持久化它;源失效时 `ApiConfig.getSource(key)` 返回 null → 打开详情页会空态(已做保护,不崩溃)。
- `type`:决定详情/列表走哪条路(`SourceViewModel.getDetail`):
  - `type == 3` → 通过 `jar` 下载 Spider 并用 `DexClassLoader` 加载,调 `sp.detailContent(ids)` 等。
  - `type == 0/1/4` → 直接 HTTP 请求 `api`(ac=videolist / detail)解析 XML/JSON。
- `jar`:Spider 插件包,本质是 zip(内含 classes.dex),下载后按 `key` 缓存。

---

## 3. 解析核心:`ApiConfig.FindResult`

入口:`ApiConfig.FindResult(String json, String configKey)`,兼容各种"非标准"配置响应:

1. **stripJsonNoise**(`ApiConfig.stripJsonNoise`):去掉 UTF-8 BOM(`\ufeff`)和开头的 `//` 注释行(老刘备、小盒子、小马等源配置带注释,Gson 不支持注释,必须先清理)。
2. **图片+base64 套路**(饭太硬类):响应 = 一张 JPEG 图片字节 + `**` + base64(真正的 JSON)。
   定位正则:`[A-Za-z0]{8}\*\*`(8 个字母 + 两个星号),命中后取 `**` 之后的 base64 段解码:
   ```java
   Pattern pattern = Pattern.compile("[A-Za-z0]{8}\\*\\*");
   content = content.substring(content.indexOf(matcher.group()) + 10);
   content = new String(Base64.decode(content, Base64.DEFAULT));
   ```
3. **AES 加密**(极少):`2423...2324` 包裹的走 CBC;否则尝试用 `configKey` 做 ECB 解密。
4. 最终必须 `AES.isJson(content)` 通过才算解析成功。

同一个"图片+**+base64"套路还用于 **jar 伪装成 .jpg** 的场景(`ApiConfig.getImgJar`):jar 字段指向 `.jpg`,内容同样是图片+**+base64(jar),下载后先解码再当 zip 处理。

---

## 4. 饭太硬问题专项记录(2026-08-15)

### 现象
- 配置地址 `http://www.饭太硬.net/tv`(中文域名),App 内一直解析不到、搜索无结果。
- 用 PC 端工具直连该域名:返回固定 **20479 字节的 JPEG 图片**,不是 JSON,也没有 `**` base64 后缀。

### 结论(重要)
1. **不是 App 的 bug**:20479 字节纯 JPEG 且无 `**` 段 → 不是"图片+base64"套路,而是源对该 IP 的**风控/反爬**返回。
2. **风控与 IP/网络环境强相关**:当前机器走 aTrust VPN(公司网络)出口 IP 被饭太硬拉黑;用户手机/家里网络正常。同一份代码在正常网络下饭太硬可正常解析。
3. 饭太硬多个域名 `.net / .cc / .cf / .tv` 行为一致(同一套风控),换域名无意义。
4. 域名是**中文域名(IDN)**:OkHttp 会自动做 punycode 编码(`饭太硬.net` → `xn--...`),这是正常的;不要因请求头里出现 punycode 而误判。

### 经验
- 判断"源挂了"还是"被风控":用**不同网络**(手机流量/其他设备)复测同一地址。
- 判断"图片+base64 套路"与"纯风控图片":看响应里有没有 `[A-Za-z0]{8}\*\*` 特征串;有 → 走解码;没有 → 是风控图。
- 饭太硬类的解析逻辑(正则解码)在 `ApiConfig.FindResult` 里是现成的,不需要再改代码。

---

## 5. 已知问题源清单(复查用)

| 源 | 状态 | 说明 |
|---|---|---|
| 饭太硬 `饭太硬.net/.cc/.cf/.tv` | ⚠️ 网络受限 | 20479 字节 JPEG = IP 风控;家庭网络正常。非 App 问题 |
| `nsvip.060028.xyz` | ❌ 已死 | DNS NXDOMAIN,源不存在,可删除订阅 |
| 摸鱼儿 `摸鱼儿.cc` | ✅ 正常 | 配置为 gzip + BOM + 注释,stripJsonNoise 可处理 |
| 王二小 `new.王二小放牛娃.top` | ✅ 正常 | 同上 |
| 老刘备 `raw.liucn.cc/box/m.json` | ✅ 正常 | 带 `//` 注释,BOM,已处理 |
| 小马 `szyyds.cn/tv/x.json` | ✅ 正常 | 同上 |
| 小盒子单仓 `xhztv.top/xhz` | ✅ 正常 | 同上 |

> 已验证:这些源配置解码后是合法 JSON、jar 是真实 zip 且含 classes.dex、md5 与配置一致。

---

## 6. 排查方法论(从现象到结论)

1. **先确认源本身可用性**:浏览器或 curl 直连配置地址,看返回内容类型:
   - JSON/文本 → 源正常,问题在 App 侧。
   - JPEG 图片 → 区分:含 `**` base64 段 = 套路需解码;纯图片 = 风控(换网络复测)。
   - 404/超时/NXDOMAIN → 源挂了,删除或换源。
2. **看 App 日志**:`运行日志`(我的-设置)里 loadJar/loadConfig 的失败信息会带真实异常(超时/HTTP 码/解析错误)。
3. **检查域名解析与编码**:IDN 中文域名被转 punycode 是正常行为。
4. **检查 jar**:下载后必须是 zip 且含 `classes.dex`;Android 8+ 加载前需 `jarFile.setReadOnly()`(否则 "Writable dex file not allowed")。
5. **区分三类原因**:
   - 源挂了(所有网络都不行)→ 换源。
   - 网络风控(特定 IP/网络不行)→ 换网络验证,App 无需改。
   - App 解析 bug(源正常但 App 解不出)→ 对照本文第 3 节解析链路排查。

---

## 7. 相关代码位置索引

| 功能 | 位置 |
|---|---|
| 订阅配置解析入口 | `app/src/main/java/com/github/tvbox/osc/api/ApiConfig.java` |
| 配置解码(去噪/图片base64/AES) | `ApiConfig.FindResult` / `stripJsonNoise` / `getImgJar` |
| Spider jar 加载 | `com/github/catvod/crawler/JarLoader.java`、`JsLoader.java` |
| 详情/列表请求分发 | `app/src/main/java/com/github/tvbox/osc/viewmodel/SourceViewModel.java`(getDetail 等) |
| HTTP 封装(okhttp4) | `app/src/main/java/com/github/tvbox/osc/util/HttpClient.java` |
| 默认订阅(本地打包用,gitignored) | `app/src/main/assets/default_subscriptions.json` |
